composer.phar
/vendor/
.idea
composer.lock
.vscode
#smile/elasticsuite puts some fonts under /lib/, so ignore that directly explicitly so we don't accidentally commit them here
/lib/
/.php-cs-fixer.cache
/app/etc/vendor_path.php