<?php
	$product = $block->getProduct();
	$reasonsToBuy = $product->getData('sinch_reasons_to_buy');
	$reasonsToBuyArray = explode("|||", $reasonsToBuy);
	if ( !empty($reasonsToBuy) ) :
		echo "<div class='sitc-reasons-to-buy'><ul>";
		foreach($reasonsToBuyArray as $reason)
		{
			echo "<li>" . $reason . "</li>";
		}
		echo "</ul></div>";
	endif;
?>